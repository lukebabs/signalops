apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: signalops-production-route
  namespace: signalops-app
  labels:
    app.kubernetes.io/name: signalops-production-route
    app.kubernetes.io/component: mesh-route
    signalops.syncratic.io/plane: app
spec:
  hostnames:
    - signalops.syncratic.io
  parentRefs:
    - name: public-ingress
      namespace: istio-system
      sectionName: http
    - name: public-ingress
      namespace: istio-system
      sectionName: signalops-production-https
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /v1
        - path:
            type: PathPrefix
            value: /healthz
        - path:
            type: PathPrefix
            value: /readyz
      backendRefs:
        - name: signalops-gateway-production
          port: 8080
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: signalops-web-production
          port: 8080
