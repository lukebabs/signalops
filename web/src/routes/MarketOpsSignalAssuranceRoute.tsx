import { MarketOpsSignalAssuranceEffectivenessPanel } from "./MarketOpsSignalAssuranceEffectivenessPanel";
import { useMemo, useState } from 'react'; import { useMarketOpsSignalAssuranceAssertions } from '../api/queries'; import { useTenant } from '../auth/session'; import { EmptyState, ErrorState, LoadingState } from '../components/States'; import { RefreshButton } from '../components/RefreshButton'; import { formatUtc } from '../lib/format'; const title = (value: string) => value.replace(/_/g, ' ').toLowerCase().replace(/\b\w/g, (letter: string) => letter.toUpperCase()); const tone = (state: string) => state === 'MATERIALIZED' ? 'border-green-200 bg-green-50 text-green-700' : state === 'INVALIDATED' ? 'border-red-200 bg-red-50 text-red-700' : state === 'EXPIRED' ? 'border-gray-200 bg-gray-50 text-gray-600' : 'border-blue-200 bg-blue-50 text-blue-700'; export function MarketOpsSignalAssuranceRoute() { const tenantId = useTenant(); const [symbol, setSymbol] = useState(''); const [mode, setMode] = useState(''); const [state, setState] = useState(''); const filter = useMemo(() => ({ tenant_id: tenantId, symbol: symbol.trim().toUpperCase() || undefined, evaluation_mode: mode || undefined, state: state || undefined, limit: 100 }), [tenantId, symbol, mode, state]); const assertions = useMarketOpsSignalAssuranceAssertions(filter); const active = Boolean(symbol || mode || state); const clear = () => { setSymbol(''); setMode(''); setState(''); }; return <div className="space-y-3"><div className="flex flex-wrap items-start justify-between gap-3"><div><h1 className="text-lg font-semibold">Signal Assurance</h1><p className="max-w-3xl text-xs text-gray-500">Post-confirmation evidence for MarketOps signals. Each assertion preserves its resolved validation contract, immutable baseline, and point-in-time provenance. Research-only results are not trade instructions.</p></div><RefreshButton onClick={() => void assertions.refetch()} loading={assertions.isFetching} /></div><div className="flex flex-wrap items-end gap-3 rounded border border-gray-200 bg-gray-50 p-3"><label className="text-xs font-medium text-gray-700">Symbol<input value={symbol} onChange={(event) => setSymbol(event.target.value)} placeholder="Ticker" className="mt-1 block w-28 rounded border border-gray-300 bg-white px-2 py-1.5 font-mono text-sm" /></label><label className="text-xs font-medium text-gray-700">Mode<select value={mode} onChange={(event) => setMode(event.target.value)} className="mt-1 block rounded border border-gray-300 bg-white px-2 py-1.5 text-sm"><option value="">All modes</option><option value="RESEARCH">Research</option><option value="LIVE">Live</option><option value="BACKTEST">Backtest</option></select></label><label className="text-xs font-medium text-gray-700">State<select value={state} onChange={(event) => setState(event.target.value)} className="mt-1 block rounded border border-gray-300 bg-white px-2 py-1.5 text-sm"><option value="">All states</option><option value="ACTIVE">Active</option><option value="MATERIALIZED">Materialized</option><option value="INVALIDATED">Invalidated</option><option value="EXPIRED">Expired</option><option value="SUPERSEDED">Superseded</option></select></label>{active ? <button type="button" onClick={clear} className="rounded border border-gray-300 bg-white px-3 py-1.5 text-xs font-medium text-gray-700 hover:bg-yellow-50">Clear filters</button> : null}<span className="pb-1 text-xs text-gray-500">{assertions.data?.assertions.length ?? 0} assertions</span></div>{assertions.isLoading ? <LoadingState /> : assertions.isError ? <ErrorState error={assertions.error} /> : (assertions.data?.assertions.length ?? 0) === 0 ? <EmptyState message="No assurance assertions yet. A successful reviewed MarketOps signal materialization creates the first RESEARCH assertion automatically." /> : <div className="overflow-x-auto rounded border border-gray-200 bg-white"><table className="min-w-full text-sm"><thead className="bg-gray-50 text-left text-xs uppercase tracking-wide text-gray-500"><tr><th className="px-3 py-2">Signal</th><th className="px-3 py-2">Direction</th><th className="px-3 py-2">State</th><th className="px-3 py-2">Mode</th><th className="px-3 py-2">Validation contract</th><th className="px-3 py-2">Confirmed</th><th className="px-3 py-2">Audit</th></tr></thead><tbody className="divide-y divide-gray-100">{assertions.data?.assertions.map((assertion) => <tr key={assertion.assertion_id} className="hover:bg-gray-50"><td className="px-3 py-2"><div className="font-mono font-semibold">{assertion.symbol}</div><div className="text-xs text-gray-500">{title(assertion.signal_type)}</div></td><td className="px-3 py-2 text-xs font-medium">{title(assertion.direction)}</td><td className="px-3 py-2"><span className={`inline-flex rounded border px-1.5 py-0.5 text-[11px] font-medium ${tone(assertion.state)}`}>{title(assertion.state)}</span></td><td className="px-3 py-2 text-xs">{title(assertion.evaluation_mode)}{assertion.evaluation_run_id ? <div className="mt-1 font-mono text-[11px] text-gray-500">{assertion.evaluation_run_id}</div> : null}</td><td className="px-3 py-2 text-xs"><div className="font-mono">{assertion.validation_contract_id}</div><div className="text-gray-500">v{assertion.validation_contract_version}</div></td><td className="whitespace-nowrap px-3 py-2 text-xs text-gray-600">{formatUtc(assertion.confirmed_at)}</td><td className="px-3 py-2"><details><summary className="cursor-pointer text-xs text-brand-700 underline">Baseline</summary><pre className="mt-2 max-w-md overflow-auto rounded bg-gray-50 p-2 text-[10px] text-gray-700">{JSON.stringify({ baseline_snapshot: assertion.baseline_snapshot, baseline_provenance: assertion.baseline_provenance }, null, 2)}</pre></details></td></tr>)}</tbody></table></div>}<MarketOpsSignalAssuranceEffectivenessPanel /></div>; }
