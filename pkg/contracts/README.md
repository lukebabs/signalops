# SignalOps Shared Contracts

This package will contain Go representations of the cross-runtime contracts
defined in `contracts/`.

Initial contract families:

- events
- artifacts
- signals
- pipelines
- rules
- features
- algorithms

The Go core platform and Python algorithm workers must use compatible schema
versions for broker payloads and internal APIs.

