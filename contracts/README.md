# SignalOps Contract Schemas

This directory is the source location for cross-runtime event and API
contracts shared by the Go core platform and Python algorithm workers.

Contracts must be versioned and must preserve replay compatibility.

Initial contract families:

- `events/`
- `artifacts/`
- `signals/`
- `pipelines/`
- `rules/`
- `features/`
- `algorithms/`

