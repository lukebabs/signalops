# Console General Use Case

This folder is for the default SignalOps Console use case:

- `app_id=console`
- `use_case=general`

Use this area for Console-specific behavior that is not tied to a specialized app such as MarketOps. Generic platform contracts still belong in top-level documentation.

Current status:

- No dedicated subfolders are needed yet. Add `architecture/`, `api/`, `frontend/`, `operations/`, or `gates/` here when Console-specific documentation grows beyond global docs.
